# nginx reverse proxy configuration for sprite
#
# Routes:
#   /code/*   → code-server :8443 (VS Code IDE)
#   /email/*  → box-agent :9999 (email webhooks)
#   /agent/*  → box-agent :9999 (agent API)
#   /health   → box-agent :9999 (health check)
#   /*        → agent-app :3000 (Next.js user app)
#
# Install: sudo cp agent-proxy.conf /etc/nginx/nginx.conf
# Test: nginx -t
# Reload: sudo nginx -s reload

events {
    worker_connections 1024;
}

http {
    include mime.types;
    default_type application/octet-stream;
    sendfile on;
    keepalive_timeout 65;

    # Logging
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;

    # Upstream: box-agent HTTP server
    upstream box_agent {
        server 127.0.0.1:9999;
        keepalive 8;
    }

    # Upstream: agent-app Next.js server
    upstream agent_app {
        server 127.0.0.1:3000;
        keepalive 8;
    }

    # Upstream: code-server IDE
    upstream code_server {
        server 127.0.0.1:8443;
        keepalive 8;
    }

    server {
        listen 8080;
        server_name _;

        # Increase buffer sizes for API responses
        proxy_buffer_size 128k;
        proxy_buffers 4 256k;
        proxy_busy_buffers_size 256k;

        # box-agent: email endpoints (webhooks from main server)
        location /email/ {
            proxy_pass http://box_agent;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_read_timeout 120s;
            proxy_connect_timeout 10s;
        }

        # box-agent: agent API endpoints
        location /agent/ {
            proxy_pass http://box_agent;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_read_timeout 300s;  # Longer timeout for agent operations
            proxy_connect_timeout 10s;
        }

        # box-agent: health check
        location /health {
            proxy_pass http://box_agent;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_connect_timeout 5s;
            proxy_read_timeout 5s;
        }

        # code-server: VS Code IDE
        location /code/ {
            proxy_pass http://code_server/;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            # WebSocket support for code-server
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_cache_bypass $http_upgrade;

            # Long timeout for IDE sessions
            proxy_read_timeout 86400s;
            proxy_connect_timeout 10s;
        }

        # agent-app: everything else goes to Next.js
        location / {
            proxy_pass http://agent_app;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            # WebSocket support for HMR in development
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_cache_bypass $http_upgrade;

            # Timeouts
            proxy_read_timeout 60s;
            proxy_connect_timeout 10s;
        }

        # Next.js static files
        location /_next/static/ {
            proxy_pass http://agent_app;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            expires 1y;
            add_header Cache-Control "public, immutable";
        }

        # Error pages
        error_page 502 503 504 /50x.html;
        location = /50x.html {
            root /usr/share/nginx/html;
            internal;
        }
    }
}
