{
  "name": "box-agent",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build --compile --minify --sourcemap ./src/index.ts --outfile dist/box-agent",
    "build:linux": "bun build --compile --target=bun-linux-x64 --minify ./src/index.ts --outfile dist/box-agent-linux-x64",
    "start": "bun run src/index.ts",
    "typecheck": "tsgo --noEmit",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "bun run src/db/migrate.ts",
    "db:studio": "drizzle-kit studio",
    "mcp:inspect": "bunx --bun @modelcontextprotocol/inspector http://localhost:33002/mcp"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.19",
    "@hono/mcp": "^0.2.3",
    "@modelcontextprotocol/sdk": "^1.13.0",
    "@orpc/client": "catalog:",
    "@orpc/openapi": "catalog:",
    "@orpc/openapi-client": "^1.13.4",
    "@orpc/server": "catalog:",
    "@orpc/zod": "catalog:",
    "@vps-claude/api": "workspace:*",
    "@vps-claude/logger": "workspace:*",
    "@vps-claude/shared": "workspace:*",
    "dotenv": "catalog:",
    "drizzle-orm": "catalog:",
    "hono": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/bun": "catalog:",
    "@vps-claude/config": "workspace:*",
    "drizzle-kit": "catalog:"
  }
}
